<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CAMERA is for react-native-image-picker's launchCamera (AI photo diagnosis, Phase 10a).
         Gallery selection (launchImageLibrary) needs no runtime permission on Android 13+ via the
         system photo picker. Declared optional so the app installs on camera-less devices. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- RECORD_AUDIO is for speech-to-text (Phase 10b, react-native-speech-recognition-kit) on the
         describe-step mic and the chat mic. Microphone is optional so the app still installs on
         mic-less devices; the UI degrades gracefully when STT is unavailable. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Location is for geo-matching: pairing a customer's service request with nearby eligible
         workers, and letting a worker publish their position when they go online. Requested at the
         POINT OF USE (customer taps "use my current location", worker goes online), never at launch
         — see src/services/locationService.js.

         COARSE is declared alongside FINE so a user who grants only approximate location still gets
         working distance matching; a few hundred metres of error is immaterial when the operating
         radius is 10 km. Both are optional in practice: every screen falls back to the saved address
         if permission is refused, so the app remains fully usable without them.

         NO background location permission is requested. Tracking stops when the worker goes
         offline, so there is no legitimate need for ACCESS_BACKGROUND_LOCATION, and asking for it
         would be a privacy overreach on a workers' cooperative app. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- Android 11+ package visibility: without this, SpeechRecognizer.isRecognitionAvailable()
         can't see the on-device speech-recognition service and returns false even when Google
         Speech Services is installed. Required for the STT engine to be detected. -->
    <queries>
      <intent>
        <action android:name="android.speech.RecognitionService" />
      </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">

      <!-- Google Maps SDK for Android key (Phase 10c, LiveTrackingMap via react-native-maps).
           The value comes from GOOGLE_MAPS_API_KEY in .env: react-native-config's dotenv.gradle
           emits every .env key as a string resource (resValue), so @string/GOOGLE_MAPS_API_KEY
           resolves at build time. SECURITY: this key ships in the APK — restrict it in Google
           Cloud Console to package com.sahakarsevamobile + the app's SHA-1 fingerprints. -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="@string/GOOGLE_MAPS_API_KEY" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
